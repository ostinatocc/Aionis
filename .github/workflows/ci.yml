name: CI

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:
    inputs:
      sdk_ref:
        description: "Standalone @aionis/sdk ref used by ownership checks"
        required: false

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  runtime:
    name: Runtime checks
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      AIONIS_SDK_REPO: ${{ github.workspace }}/external/aionis-sdk
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "24"
          cache: npm

      - name: Resolve standalone SDK ref
        id: sdk-ref
        env:
          SDK_REF_OVERRIDE: ${{ inputs.sdk_ref }}
        run: |
          node --input-type=module -e '
            import fs from "node:fs";
            const releaseTrain = JSON.parse(fs.readFileSync("release-train.json", "utf8"));
            const fallbackRef = releaseTrain.packages.sdk.source_ref || "main";
            fs.appendFileSync(process.env.GITHUB_OUTPUT, `ref=${process.env.SDK_REF_OVERRIDE || fallbackRef}\n`);
          '

      - name: Checkout standalone SDK contracts
        uses: actions/checkout@v4
        with:
          repository: ostinatocc/aionis-sdk
          ref: ${{ steps.sdk-ref.outputs.ref }}
          path: external/aionis-sdk

      - name: Install dependencies
        run: npm ci

      - name: Typecheck
        run: npm run -s typecheck

      - name: Release artifact gate
        run: node scripts/ci/release-artifact-gate.mjs --check
        env:
          AIONIS_RELEASE_SDK_REPO: ${{ github.workspace }}/external/aionis-sdk

      - name: Release docs version check
        run: node --test scripts/ci/release-version-docs.test.mjs

      - name: Standalone SDK contract check
        run: npm run -s sdk:check

      - name: Runtime complexity gate
        run: npm run -s complexity:check

      - name: Lite test suite
        run: npm run -s lite:test

      - name: Lite public smoke
        run: npm run -s lite:smoke

  minimum-node:
    name: Node 22 minimum compatibility
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up minimum supported Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22.5.0"
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Typecheck
        run: npm run -s typecheck

      - name: Lite public smoke
        run: npm run -s lite:smoke
